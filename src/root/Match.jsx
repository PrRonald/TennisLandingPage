import { TableMetrics } from "../components/match/TableMetrics";

export const Match = () => {
    return (

            <section className="w-full h-[50vh] flex flex-col place-content-center items-center bg-blue-950">
                <TableMetrics />
            </section>
    );
}